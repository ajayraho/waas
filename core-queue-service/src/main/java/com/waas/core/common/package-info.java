/**
 * <b>Common</b> — cross-cutting plumbing only: error model, clock, Redis key naming.
 * <p>Must not depend on any domain package. Every domain may depend on it.
 */
package com.waas.core.common;
